<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# App 0.5.0 release preparation

Prepared 2026-09-05 for `app-v0.5.0`, covering desktop and server as source.
This is a pre-1.0 feature and storage upgrade. The scaffold CLI is not released.
The [release rules](../../specs/packages/release.md) remain the gate authority.

## Review

Reviewed all 38 commits from `app-v0.4.0` through
`0f736935c091f4b488747469e97d2e4a91b2d682` using:

```sh
git log --reverse --format='%H %s%n%b' app-v0.4.0..0f73693
git diff app-v0.4.0..0f73693
```

The [changelog](../../CHANGELOG.md) includes shared storage/recovery, Git
selection and rebinding, portable Library files, token-only usage and the
graph-pane fix. Editorial, ADR bookkeeping and fixture-only commits need no
separate release entries. Later implementation and release fixes:

| Commit | Release impact |
| --- | --- |
| `00cf071` | Shared Playbook lifecycle, continuation/recovery/deletion, retained ownership after failed cleanup, and consistent Git/core ledger validation. |
| `adb9ca1` | Confirmed Retry/Discard, saved graph context, history replacement/reconnect and draft/queue preservation. |
| `d87ed6c` | Smoke runs workspace coverage once and waits for actual session cleanup. |
| `6ca0b70` | Canonical POSIX/Windows recorded aliases preserve project identity; equivalent native registration paths no longer mint duplicate IDs. |
| `dd62722` | Active or unprovable external ownership keeps history visible and prevents desktop mutations; lease retirement reveals recovery without another replay write. |
| `b08a5dc` | macOS/Linux hosts require private POSIX storage; Windows keeps scaffold/UI coverage and an actual native startup-refusal CI gate. |
| `db53b8e` | Ordinary default-home startup discovers the former CLI store through shared migration; explicit home/session choices remain isolated. |
| `e5c9799` | Live smoke can reuse an unchanged successful build; native ABI setup/restoration remain mandatory. Driver syntax check passed. |
| `44ed76b` | Fixtures compare complete replay, including context and shutdown records, after durable settlement; all five changed cases passed without product changes. |
| `ed3ec8c` | Smoke resumes verified stages without repeating unchanged builds/tests; the remaining candidate stages passed. |
| `6f11827` | Records candidate verification and remaining release gates. |
| `8737492` | An optional five-minute pause retains the live result for native review; timeout fails and restores the Node ABI. |
| `0f73693` | Live acceptance requires actual player text, thinking or tool activity; initialization alone cannot pass. Four stream regressions passed. |

The prepared changelog now includes ownership and platform support. Final
review still covers the dependency diff and any subsequent fixes.

## Upgrade boundaries

Stop legacy desktop/server and CLI writers before migration. Keep the retained
originals and do not run older writers against converted files. Track sessions
in Git only after migration removes provider tokens from portable records.
Ordinary default-home startup imports `$XDG_STATE_HOME/playbook/sessions`, or
`~/.local/state/playbook/sessions` when XDG is unset, retaining originals in migration receipts.
An explicit home or session directory suppresses that discovery.

Desktop/server hosts require macOS or Linux and private POSIX permissions.
Windows supports the scaffold CLI and browser access to a supported server.

Choose a complete session when both branches changed it; Git does not combine
continuations or undo external effects. Use one device per active session.
Schema 7 does not relocate repository/module paths: different paths allow
history only. Compatible paths still require runtime and effect reconciliation.

## Existing implementation evidence

These results used the development dependency closure. They establish the
changed behavior; they do not replace final registry-only release acceptance.
Tests were rerun only for changed inputs or corrected failing cases.

| Scope | Recorded result |
| --- | --- |
| Application storage, config, compilation and Git | 55 scoped checks passed; later rebinding, unknown-format and dispatch-boundary regressions passed. |
| Shared Store and SessionManager fixtures | 16 Store and 4 session checks passed. |
| Remaining core files | All 97 cases passed across bounded follow-ups, including ledger recovery and failed-cleanup ownership. |
| Cross-host integration | Real-shell continuation, CLI-written history/deletion, Retry/Discard, binding changes and replacement notifications passed. |
| Browser journeys | All 40 hermetic cases passed across bounded runs, including restart, history, deletion and explicit recovery. |
| UI follow-ups | Saved graphs, refused queued sends and reconnect replacement passed; UI build/typecheck passed. |
| Portable project paths | 22/22 application-storage and Store cases passed (`portable-path-tests.log`), including foreign aliases, canonical registration and restart. |
| External ownership | 2/2 core recovery cases and 109/109 affected UI cases passed; core/UI builds and lint passed (`core-writer-state-recovery1.log`, `spex-external-writer-ui-final-tests.log`). |
| Platform boundary | 1/1 refusal case passed locally; config/data paths stayed absent. Windows itself remains unverified until CI (`platform-refusal-test.log`). Workflow structure and spec lint passed. |
| Former CLI store discovery | 3/3 core cases passed: ordinary default, explicit home, explicit sessions (`core-legacy-default-tests2.log`). Core build passed. |
| Structural checks | Core build and `spex lint` passed before this metadata preparation. |

Initial failures identified obsolete fixtures, a chat-source dedup defect and
settlement/cleanup expectations; fixes and their targeted checks are recorded
in the implementation commits. Loopback-denied runs were repeated with the
required local socket permission. The first discovery run passed 2/3: its
synthetic legacy fixture was not a valid migration input. Replacing it with a
real shared-host schema-6 projection and adjacent replay produced the 3/3 result.
No tests or builds ran for this evidence update.

## Packed-candidate acceptance

The baseline Spex `ed3ec8c` used packed Playbook `a6ff03b9` installed without saving and
published Cligent `0.25.0`. All 99 Playbook files match the candidate's
`files.json`; the installed directory is not a symlink. The tracked dependency
lock remains unchanged until Playbook is public. This proves the candidate
integration, not completed registry adoption.

| Check | Result |
| --- | --- |
| Build and spec lint | Passed in `spex-candidate-smoke1.log`. |
| Workspace tests | 833 unique cases passed across bounded runs: scripts 18, CLI 133, core 210, UI 453, desktop 9, server 10. |
| Corrected fixtures | Initial core run passed 208/210. `44ed76b` corrected complete-replay/settlement expectations; all five changed cases passed (`core-shutdown-fixture-tests.log`). No product code changed. |
| Remaining smoke stages | 40/40 browser journeys, core round-trip, installed packed-CLI journeys, Electron render and Node ABI restoration passed (`spex-candidate-smoke-resume1.log`). |
| CLI pack inspection | 36 production files, zero unexpected files (`spex-candidate-cli-pack.json`). |
| Startup visual | `spex-candidate-desktop.png` was inspected: legible startup, no overflow, `consoleErrors: []`. This does not complete the interactive manual checklist. |

Logs and the screenshot are retained under `/tmp/spex-unified-implementation/`.
Previously successful unchanged inputs were not rerun after the fixture fixes.
Spec lint after the IR-074 evidence update passed with no problems
(`spex-release-evidence-ir-lint.log`); this update ran no tests or builds.

## Current candidate and manual evidence

Spex `0f73693` now uses Playbook `3444353`; all 99 installed files match.
Compared with `d3e60e4`, only `bin/session-store.js` changed, adding the
`nativeSessionId` sanitizer alias. Three Playbook regressions cover that change;
the prior 88 passing core checks and 18 passing browser journeys on `d3e60e4`
remain recorded. Declarations, assets and transitive dependencies are unchanged,
so the existing builds were reused.

Playbook passed 1,743 tests, [exact-candidate CI](https://github.com/sublang-ai/playbook/actions/runs/34001590898),
and all six live acceptance cases. Its manual terminal UX check remains pending:
CUA refuses Terminal control, and the owner has the prepared launch request.

Spex's native navigation pass completed fresh readiness, Academy creation/reopen,
Specs search/citations/jumps, built-in source/graphs, compile prefill, persisted
Captain settings, empty Dashboard and Light/Dark inspection. It submitted no
provider turn; the original Light theme and Node ABI were restored. Detailed
isolation and visual evidence is in `spex-manual-acceptance.md` beside the logs.

The first live/manual attempt accepted adapter initialization as provider output;
it does **not** satisfy the live gate. Native Dock control timed out after
30 minutes; the driver's five-minute manual watchdog failed and restored the
Node ABI (`spex-live-desktop-manual.log`). `0f73693` fixes the false-positive
output check; all four regressions passed (`spex-live-output-regression.log`).
The corrected live run **passed** on Spex `0f73693` with Playbook `3444353`
(`spex-live-provider-output.log`): `dev.coder` emitted provider text before abort,
process `13263` exited `0`, and the Node ABI was restored.

Retained replay in `spex-live-records-3444353/verification.json` reports
`integrityValid: true` and `reasons: []`. Provider events are `init`, `text`, then
`done` with interrupted status. Provider usage reports only `toolUses: 0`, so
the UI correctly invents no token usage. History text was readable; the saved
Code graph expanded with its failed state active. Retry/Discard showed the saved
input while ordinary Send and queue controls were disabled. Dashboard showed
All clear/idle with attention `0`, matching `ledger.get` badge `0`.

The provider-free native fixture completed a real waiting-question flow:
Dashboard opened the correct question, and replying Continue cleared attention.
The saved ledger changed from badge `1` to badge `0` with `attention: []`
(`native-manual/runs/check-IWGHpR/{ready.json,after-reply.json}`). Process `20880`
exited `0` after graceful quit and restored the Node ABI (`native-manual/first-run.log`).
Native OS badge and banner observations remain pending; computed badge state is
not proof of the OS display. The owner has both isolated manual launchers.

## Final gates

The supplied Fable review found migration, isolation and activity defects after
the historical runs above. Review fixes added targeted migration/CLI, core/UI,
real Git effect-recovery and missing-module regressions. The final candidate
`1fb6629` resolves the published dependency closure; every gate below ran
against that commit on 2026-09-06 unless stated.

| Gate | Result |
| --- | --- |
| Published dependencies | Cligent `0.25.0` and Playbook `13.0.0` ([GitHub release](https://github.com/sublang-ai/playbook/releases/tag/v13.0.0), release workflow `34021987115`) are public on npm with provenance. |
| Installed closure and lockfile | `package-lock.json` resolves Playbook `13.0.0` and Cligent `0.25.0` from the registry with recorded integrities; both shells are `0.5.0`. |
| Automated app smoke | `npm run smoke -- --desktop` passed every stage: build, spec lint, 25 files / 458 tests, 40/40 browser journeys on one worker, core round-trip, packed-CLI journeys, Electron render, Node ABI restored. |
| Tarball hygiene | Unchanged inputs since the packed-candidate inspection: 36 production files, zero unexpected entries. |
| Live checks | `npm run smoke:desktop` passed: `dev.coder` emitted live output, abort, ended session, clean teardown, ABI restored. Playbook live acceptance passed 6/6 on its release commit. |
| Manual desktop | Completed over the served UI of a migrated copy (below); native OS badge and banner observation stays with the owner. |
| Final review and commit | [IR-074](../../specs/intents/074-unified-storage.md) closed with this record. |
| Main CI | Run [34022181526](https://github.com/sublang-ai/spex/actions/runs/34022181526) succeeded for `1fb6629`: macOS and Ubuntu on Node 20 and 22, Windows on Node 22, Ubuntu journeys. |
| Publication | `app-v0.5.0` tags the commit carrying this record once its CI succeeds; the App Release workflow publishes the changelog notes. |

## Manual checklist on migrated data

Environment: the served UI (`apps/server`) on a loopback port over a copy of the
owner's `~/.spex` plus the former CLI store, with `HOME` and `XDG_STATE_HOME`
isolated; the originals were untouched. The copy holds two desktop demo sessions,
six former CLI sessions and one unsupported schema-1 sidecar.

| Step | Observed |
| --- | --- |
| Upgrade copied desktop and CLI data | Both demo sessions list with titles and turn counts and open with their Captain pane, player panes and saved machine cards (`/decide`: 8 cards, 2 players; `/dev`: 6 cards, 3 players). Each shows "legacy desktop history lacks complete durable recovery" in place of the composer and offers only "New session". The schema-6 CLI session converted to `settled`; schema-3 sessions are history only with "legacy schema 3 has no supported recovery"; the schema-1 sidecar stays untouched and is reported as a non-blocking diagnostic. Sessions whose `cwd` has no binding stay unlisted; `validate` names all four as non-blocking. |
| CLI writer, then deletion | Deleting the empty former-CLI session from the sidebar removed both bundle files and left only its retired lease directory. Live CLI writing was not repeated; the smoke's real-shell continuation tests cover it. |
| Select Git history and rebind | The recorded Academy checkout no longer exists locally. `rebind` moved the project to a fresh repository with the old path as an alias; after relaunch the same project ID lists both sessions, Overview shows the new path and branch, and the sessions remain history only. Whole-unit selection ran in the smoke's real-Git integration tests. |
| Session history and Dashboard | Attention badge `1` is the `/dev` finished delivery awaiting a verdict; academy-demo History lists its four finished records; ended sessions show no spinner or Abort. |
| Settings | Captain effort `high` → `medium` saved, survived a reload, and the config stayed valid. |
| Playbooks | `/code`, `/review`, `/decide` and `/dev` show their source, gears and state machine links. |
| Dark theme | Sidebar mark, panes and Playbooks stay legible under `prefers-color-scheme: dark`. |
| Retry and Discard | Not repeated manually; the hermetic journeys and the earlier live evidence on `0f73693` stand. |
| Live run, first launch, Academy example, Specs | The live smoke covers the run; the browser journeys and the earlier native pass cover the rest. Specs over the migrated Academy project reported no `specs/` directory because its checkout is absent, which is the correct answer. |

The automated app smoke includes the packed CLI README journey. A separate
scaffold tag, CLI live-migration smoke, binary packaging and signing are not
required for this app release.
